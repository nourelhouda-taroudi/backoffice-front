export enum TransferStatus {
  TO_SERVE = "to serve",
  SERVED = "served",
  BLOCKED = "blocked",
  RETURNED = "returned",
  REVERSE = "reversed",
  DORMANT = "dormant",
  UNLOCK_TO_SERVE = "unlock to serve"
}

